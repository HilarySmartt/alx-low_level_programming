#include "main.h"

/**
 * _isdigit - checks for digits
 * @c: digits to be checked
 * Return: 1 or 0
 * Auth: HilarySmartt
 */

int _isdigit(int c)
{
	int boolean;

	if (c >= 0 a && c <= 9)
		boolean = 1;
	else
		boolean = 0;

	return (boolean);
}
