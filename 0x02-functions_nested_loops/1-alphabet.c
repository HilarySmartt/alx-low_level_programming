#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - Prints the alphabet in
 * lowercase, followed by a new line
 * Return: Always 0
 * by HilarySmartt
 */
void parent_alphabet()
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
}
